# Prompt Versioning

- Prompts are versioned implicitly through git history.
- Major structural changes should be noted in commit messages.
- If a prompt changes purpose, create a new file instead of overwriting.
- Avoid prompt drift by keeping prompts short and explicit.